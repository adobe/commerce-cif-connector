version: 2.1

# This is a reusable command to perform a maven release
commands:
  release:
    description: "Performs a maven release in the specified folder"
    parameters:
      folder:
        type: string
    steps:
      - checkout
      - restore_cache:
          keys: 
            - maven-repo-v1-{{ checksum "pom.xml" }}
            - maven-repo-v1-
      - run:
          name: Release
          # Only performs a 'mvn deploy' after the 'mvn release:prepare' because circleCI
          # already checks out the git tag like 'mvn release:perform' would do. 
          command: |
            echo $GPG_PRIVATE_KEY | base64 --decode | gpg --batch --import
            cd << parameters.folder >>
            mvn -B -s /home/circleci/project/.circleci/settings.xml clean deploy -P release-sign-artifacts
            rm -rf /home/circleci/.gnupg
        
jobs:
  build:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - checkout
      - restore_cache:
          keys: 
            - maven-repo-v1-{{ checksum "pom.xml" }}
            - maven-repo-v1-
      - run:
          name: Build
          command: |
            java -version
            mvn -v
            mvn -B clean install -s .circleci/settings.xml
      - save_cache:
          paths:
            - ~/.m2
          key: maven-repo-v1-{{ checksum "pom.xml" }}
      - store_test_results:
          path: target/surefire-reports
      - store_artifacts:
          path: target/surefire-reports
          
  release_cif_connector_parent:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "parent"
          
  release_cif_connector_graphql:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "bundles/cif-connector-graphql"
            
  release_cif_virtual_catalog:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "bundles/cif-virtual-catalog"
            
  release_cif_connector_content:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "content/cif-connector-content"
            
  release_cif_virtual_catalog_content:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "content/cif-virtual-catalog-content"
            
  release_cif_connector_all:
    docker:
      - image: circleci/openjdk:8-jdk
    steps:
      - release:
          folder: "all"

workflows:
  version: 2
  build-and-release:
    jobs:
      - build:
          filters:
            tags:
              only: /.*/
              
      - release_cif_connector_parent:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-connector-parent-.*/
              
      - release_cif_connector_graphql:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-connector-graphql-.*/
              
      - release_cif_virtual_catalog:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-virtual-catalog-.*/
              
      - release_cif_connector_content:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-connector-content-.*/
              
      - release_cif_virtual_catalog_content:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-virtual-catalog-content-.*/
              
      - release_cif_connector_all:
          requires:
            - build
          filters:
            branches:
              ignore: /.*/
            tags:
              only: /^cif-connector-all-.*/
